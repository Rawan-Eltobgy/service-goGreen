{
    "name": "gogreen-service",
    "version": "1.0.0",
    "description": "The request to the route `/vehicles` returns all the paginated vehicles with default values of page and limit. The query parameters that can be used to set the pagination criteria are: - `page:` The page of the resource to be fetched. Defaults to 1. [NUMBER] - `limit:` The number of items to be returned in a single page. Defaults to 3. [NUMBER]",
    "main": "index.js",
    "scripts": {
        "start": "nodemon",
        "test": "jest"
    },
    "author": "Rawan Eltobgy",
    "license": "MIT",
    "dependencies": {
        "chai": "^4.3.4",
        "chai-http": "^4.3.0",
        "cookie-parser": "^1.4.5",
        "express": "^4.17.1",
        "mocha": "^9.0.1",
        "morgan": "^1.10.0",
        "nodemon": "^2.0.7",
        "supertest": "^6.1.3"
    },
    "devDependencies": {
        "jest": "^27.0.5"
    }
}
